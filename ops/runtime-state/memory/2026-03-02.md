# 2026-03-02

## Memory System Fixed

- Diagnosed memory gap: no daily notes written since Feb 17
- Old session transcripts were being cleaned up on daily reset (.deleted suffix)
- Enabled session transcript indexing in config (experimental.sessionMemory + sources)
- Backfilled memory files from old transcripts (Feb 16–Mar 1)
- Updated MEMORY.md with agent team, projects, infrastructure

## Gemini Flash Fallback

- Added google/gemini-2.0-flash as fallback model (alias: flash)
- Uses existing Gemini API key (same as embeddings)
- Fallback chain: Haiku → Sonnet → Gemini Flash

## Dashboard Org Chart

- DEVS redesigned agent dashboard from nested list to org-chart diagram
- Top-down tree: TARS at top, ARCH + STEVES below, DEVS + PATCHER under ARCH
- CSS connector lines, responsive layout
- Hannes approved: "looks good"
